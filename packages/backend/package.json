{
  "name": "@qali/backend",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "",
  "scripts": {
    "check-types": "tsc -p convex/tsconfig.json --noEmit",
    "test": "bun test convex && vitest run",
    "test:unit": "bun test convex",
    "test:integration": "vitest run",
    "dev": "convex dev",
    "dev:setup": "convex dev --configure --until-success",
    "deploy:prod": "convex deploy"
  },
  "dependencies": {
    "@qali/domain": "workspace:*",
    "convex": "catalog:",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@edge-runtime/vm": "^5.0.0",
    "@qali/config": "workspace:*",
    "@types/node": "^24.3.0",
    "convex-test": "0.0.54",
    "typescript": "catalog:",
    "vitest": "^4.1.10"
  }
}
